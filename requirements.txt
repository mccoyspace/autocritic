# Core dependencies
numpy>=1.24
Pillow>=10.0

# LLM providers (install at least one)
# anthropic>=0.40       # pip install autocritic[anthropic]
# openai>=1.0           # pip install autocritic[openai]
# google-genai>=1.0     # pip install autocritic[google]
